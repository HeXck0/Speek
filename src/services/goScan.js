import fetch from 'node-fetch';

export async const discoveryHeadReq = (url) => {
  try {
    const controller = new AbortController();
    setTimeout(() => controller.abort(), 5000);

    
  } catch(err) {

  }
}
